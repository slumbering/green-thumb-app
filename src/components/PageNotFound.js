import React from 'react'
import { Message } from 'semantic-ui-react'

const PageNotFound = () => (
    <Message negative>
      <Message.Header>We're sorry but this page has not been found</Message.Header>
    </Message>
  )

export default PageNotFound;